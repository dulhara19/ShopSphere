# Inventory Service Infrastructure

This folder contains infrastructure assets for the inventory service.

## Structure

- `docker/`: Docker image build files
- `aws/`: AWS deployment placeholders (IaC, environment docs)
- `kubernetes/`: Kubernetes manifests
- `scripts/`: operational helper scripts
